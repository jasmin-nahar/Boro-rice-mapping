# Local data directory

Create a local `Data/` directory at the repository root and place authorized input data in the paths expected by the notebooks. PlanetScope imagery, private credentials, and sensitive GPS attributes are intentionally excluded from the public repository.
